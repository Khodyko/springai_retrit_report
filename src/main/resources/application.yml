spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: spring-ai-retrit
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
      embedding:
        options:
          model: text-embedding-3-small
    vectorstore:
      pgvector:
        initialize-schema: true
        dimensions: 1536
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always

server:
  port: 8080

logging:
  level:
    com.example.retreat: INFO

retreat:
  search:
    # Настраивается вручную перед запуском / перезапуском приложения.
    # Для text-embedding-3-small типичные scores ~0.35–0.55; 0.6+ часто отсекает релевантные чанки.
    similarity-threshold: 0.4
    top-k: 4
